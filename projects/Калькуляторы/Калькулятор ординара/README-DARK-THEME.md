# 🌙 Темная тема для калькулятора ординара

Виджет автоматически адаптируется к темной/светлой теме родительской страницы.

## 🚀 Возможности

- **Автоматическое определение** темы родительской страницы
- **Мгновенная реакция** на изменения темы
- **Множественные методы** определения темы
- **Fallback** на системные настройки
- **CORS-безопасность** при работе в iframe

## 🎨 Как работает

### Методы определения темной темы:

1. **CSS классы** родительской страницы:
   - `dark`, `dark-theme`, `theme-dark`
   - `dark-mode`, `night`, `night-mode`

2. **Data-атрибуты**:
   - `data-theme="dark"`
   - `data-color-scheme="dark"`

3. **Анализ цвета фона**:
   - Автоматический расчет яркости RGB
   - Порог определения: luminance < 128

4. **Системные настройки**:
   - `prefers-color-scheme: dark`
   - Используется как fallback

## 💻 Интеграция

### Базовая интеграция

```html
<iframe 
    src="путь/к/betting-calculators/index.html"
    style="width: 100%; border: none; min-height: 300px;"
    scrolling="no">
</iframe>

<script>
// Обработчик автоматического ресайза
window.addEventListener('message', function(event) {
    try {
        const data = JSON.parse(event.data);
        
        if (data.action === 'resizeIframe') {
            const iframe = document.querySelector(data.selector);
            if (iframe && data.sizes.height) {
                iframe.style.height = data.sizes.height + 'px';
            }
        }
    } catch (e) {}
});
</script>
```

### Интеграция с CSS-фреймворками

#### Bootstrap Dark Mode
```html
<body data-bs-theme="dark">
    <!-- Виджет автоматически определит темную тему -->
    <iframe src="betting-calculators/index.html"></iframe>
</body>
```

#### Tailwind CSS
```html
<body class="dark">
    <!-- Виджет подстроится под класс 'dark' -->
    <iframe src="betting-calculators/index.html"></iframe>
</body>
```

#### Пользовательская тема
```html
<body class="my-dark-theme" data-theme="dark">
    <!-- Сработает по data-атрибуту -->
    <iframe src="betting-calculators/index.html"></iframe>
</body>
```

## 🔧 Ручное управление

### JavaScript API

```javascript
// Принудительная установка темы
window.ThemeDetector.applyTheme(true);  // темная тема
window.ThemeDetector.applyTheme(false); // светлая тема

// Получение текущей темы
const currentTheme = window.ThemeDetector.getCurrentTheme();
console.log(currentTheme); // 'dark' или 'light'

// Определение темы родительской страницы
const parentIsDark = window.ThemeDetector.detectParentTheme();
```

### События

```javascript
// Слушатель изменения темы
document.addEventListener('themeChanged', function(event) {
    const isDark = event.detail.isDark;
    console.log('Тема изменилась на:', isDark ? 'темную' : 'светлую');
});
```

## 🎯 Поддерживаемые цветовые схемы

### Светлая тема
- Фон виджета: `#ffffff`
- Текст: `#1a1a1a`
- Поля ввода: `#F4F5F7`
- Границы: `#E5E9F2`

### Темная тема  
- Фон виджета: `#2d2d2d`
- Текст: `#ffffff`
- Поля ввода: `#3a3a3a`
- Границы: `#4a4a4a`

## 📱 Тестирование

Откройте `demo-dark-theme.html` для интерактивного тестирования:

```bash
# Локальный сервер
python -m http.server 8000
# Откройте: http://localhost:8000/demo-dark-theme.html
```

## 🔍 Отладка

### Проверка определения темы

```javascript
// В консоли браузера виджета
console.log('Определенная тема:', window.ThemeDetector.getCurrentTheme());
console.log('Тема родителя:', window.ThemeDetector.detectParentTheme());
```

### Логи в консоли

- `"Cannot access parent window, using system preference"` - CORS ограничения
- `"CORS restrictions, using system theme only"` - Невозможно отследить изменения родителя
- `"themeChanged"` - Событие успешного изменения темы

## ⚡ Производительность

- **Минимальное влияние**: < 5KB JavaScript кода
- **Оптимизированное слежение**: MutationObserver с фильтрами
- **Throttling**: Игнорирование незначительных изменений
- **Автоостановка**: Проверки останавливаются через 5 секунд

## 🛠️ Кастомизация

### Добавление новых классов

Отредактируйте `darkThemeClasses` в `theme-detector.js`:

```javascript
const darkThemeClasses = [
    'dark', 'dark-theme', 'theme-dark',
    'dark-mode', 'night', 'night-mode',
    'your-custom-dark-class'  // ← добавьте свой класс
];
```

### Изменение цветов

Отредактируйте CSS переменные в `styles.css`:

```css
@media (prefers-color-scheme: dark) {
    :root {
        --calculator-bg: #your-color;
        --text-primary: #your-color;
        /* ... другие переменные */
    }
}
```

## 🐛 Известные ограничения

1. **CORS**: В cross-origin iframe может не работать определение темы родителя
2. **Fallback**: При CORS используются только системные настройки
3. **Задержка**: Первое определение может занять до 500мс для медленных страниц

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Убедитесь что родительская страница использует стандартные классы/атрибуты
3. Проверьте CORS-настройки при межсайтовой интеграции 