# 🚀 НАЧНИТЕ ОТСЮДА!

## Добро пожаловать в Betting News Parser!

Это полностью готовая к использованию система автоматического сбора и анализа новостей о букмекерской индустрии России.

---

## ⚡ Быстрый старт (3 шага, 5 минут)

### Шаг 1: Настройка

Скопируйте файл с примером настроек:

```bash
cp .env.example .env
```

**Опционально:** Если хотите Telegram уведомления, откройте `.env` и добавьте:
- `TELEGRAM_BOT_TOKEN` - токен от @BotFather
- `TELEGRAM_CHAT_ID` - ID вашего канала/чата

### Шаг 2: Запуск

**Linux/macOS:**
```bash
chmod +x run.sh
./run.sh
```

**Windows:**
```powershell
docker-compose up -d
```

### Шаг 3: Открыть дашборд

Откройте в браузере: **http://localhost**

---

## 📊 Что вы получите?

### Через 5 минут после запуска:

✅ **База данных** с 200+ источниками готова  
✅ **Первые новости** уже спарсены  
✅ **Веб-дашборд** работает с графиками  
✅ **Telegram бот** отправляет уведомления (если настроен)  
✅ **ML классификатор** категоризирует новости  

### Автоматически работает:

🔄 **Каждые 5 минут** - парсинг 200+ источников  
🔍 **Каждые 30 минут** - поиск новых источников  
📱 **Каждые 10 минут** - Telegram уведомления  
📊 **Каждый день в 9:00** - ежедневная сводка  
🧹 **Каждый день в 3:00** - очистка старых данных  

---

## 📁 Что читать дальше?

### Для быстрого старта:
👉 **QUICKSTART.md** - подробные инструкции по запуску

### Для понимания возможностей:
👉 **PROJECT_OVERVIEW.md** - обзор всех функций

### Для разработчиков:
👉 **README.md** - полная документация  
👉 **ARCHITECTURE.md** - архитектура системы  

### Для просмотра структуры:
👉 **FILES_SUMMARY.txt** - описание всех файлов

---

## 🎯 Основные возможности

### 1. Автоматический сбор новостей
- 200+ источников (Sports.ru, LegalBet, RBC, и др.)
- Динамический поиск новых источников через Google/Yandex
- Умная фильтрация по 100+ ключевым словам
- Дедупликация через Redis

### 2. ML/NLP анализ
- Автоматическая категоризация (7 категорий)
- Оценка срочности (0-10 баллов)
- Извлечение сущностей (компании, персоны, суммы)
- Анализ тональности

### 3. Уведомления
- Telegram Bot с форматированными сообщениями
- Топ-10 новостей каждые 10 минут
- Ежедневная сводка со статистикой
- Алерты о критических событиях

### 4. Веб-дашборд
- Real-time графики на Plotly
- Статистика по категориям
- Топ источников
- Облако ключевых слов
- Фильтрация новостей

### 5. API для интеграций
- REST API endpoints
- JSON формат данных
- Пагинация и фильтрация
- Real-time обновления

---

## 🔧 Полезные команды

### Просмотр логов:
```bash
docker-compose logs -f parser
```

### Проверка статуса:
```bash
docker-compose ps
```

### Остановка:
```bash
docker-compose down
```

### Перезапуск:
```bash
docker-compose restart
```

### Подключение к базе данных:
```bash
docker-compose exec postgres psql -U parser_user -d betting_news
```

### Запуск тестов:
```bash
docker-compose exec parser python test_parser.py
```

---

## 📊 Что внутри дашборда?

Откройте **http://localhost** и увидите:

1. **📈 Статистика**
   - Новостей за 24 часа
   - Всего новостей в базе
   - Активных источников

2. **📉 График Timeline**
   - Новости по времени (последние 48 часов)
   - Разбивка по категориям
   - Интерактивный Plotly график

3. **🥧 Круговая диаграмма**
   - Распределение по категориям
   - Данные за 24 часа

4. **📊 Bar Chart**
   - Топ-15 источников
   - Количество новостей от каждого

5. **🏷️ Облако тегов**
   - Топ-30 ключевых слов
   - Частота упоминаний

6. **📰 Список новостей**
   - Последние новости с фильтрацией
   - Ссылки на оригиналы
   - Категория и срочность

---

## 🎓 Категории новостей

| Категория | Описание | Примеры |
|-----------|----------|---------|
| **regulation** | Регулирование | ФНС, лицензии, ЦУПИС |
| **law** | Законодательство | Госдума, законопроекты |
| **legal** | Судебные дела | Суды, штрафы, блокировки |
| **tax** | Налоги | GGR, налогообложение |
| **company** | Компании | Новости букмекеров |
| **market** | Рынок | Обороты, статистика |
| **product** | Продукты | Приложения, сервисы |

---

## 🔐 Настройка Telegram (опционально)

### 1. Создайте бота
- Напишите [@BotFather](https://t.me/BotFather)
- Команда `/newbot`
- Придумайте имя и username
- Скопируйте токен

### 2. Узнайте Chat ID
- Создайте канал или группу
- Добавьте туда бота как администратора
- Напишите [@userinfobot](https://t.me/userinfobot)
- Перешлите любое сообщение из канала
- Скопируйте chat_id

### 3. Добавьте в .env
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890
```

### 4. Перезапустите
```bash
docker-compose restart
```

---

## 📡 API Endpoints

### Статистика
```bash
curl http://localhost:5000/api/stats
```

### Новости
```bash
curl "http://localhost:5000/api/news?limit=10"
```

### С фильтрацией по категории
```bash
curl "http://localhost:5000/api/news?limit=10&category=regulation"
```

### Графики
```bash
curl http://localhost:5000/api/chart/timeline
curl http://localhost:5000/api/chart/categories
curl http://localhost:5000/api/chart/sources
```

### Ключевые слова
```bash
curl http://localhost:5000/api/keywords
```

---

## ❓ Часто задаваемые вопросы

### Q: Парсер не находит новости?
**A:** Проверьте:
1. Интернет-соединение
2. Логи: `docker-compose logs parser`
3. Доступность источников

### Q: Telegram не работает?
**A:** Убедитесь:
1. Токен и chat_id правильные в .env
2. Бот добавлен в канал как администратор
3. Перезапустили после изменения .env

### Q: Дашборд не открывается?
**A:** Проверьте:
1. Контейнеры запущены: `docker-compose ps`
2. Порт 80 свободен
3. Логи dashboard: `docker-compose logs dashboard`

### Q: База данных не подключается?
**A:**
1. Проверьте контейнер: `docker-compose logs postgres`
2. Перезапустите: `docker-compose restart postgres`
3. Проверьте credentials в .env

---

## 🌟 Что можно настроить?

### В файле config.py:

**Интервалы работы:**
```python
PARSE_INTERVAL_MINUTES = 5        # Парсинг каждые N минут
SOURCE_DISCOVERY_INTERVAL = 30    # Поиск источников
```

**Параллельность:**
```python
MAX_CONCURRENT_REQUESTS = 100     # Одновременных запросов
```

**Источники:**
```python
PRIORITY_SOURCES = [
    'https://your-source.ru/news',
    # Добавьте свои источники
]
```

**Ключевые слова:**
```python
KEYWORDS = {
    'category_name': ['слово1', 'слово2', ...],
}
```

---

## 📞 Поддержка

Если что-то не работает:

1. **Проверьте логи:** `docker-compose logs -f`
2. **Читайте документацию:** README.md, QUICKSTART.md
3. **Создайте Issue** в GitHub (если есть репозиторий)

---

## 🎉 Готово!

Система запущена и работает автоматически. Наслаждайтесь!

### Что происходит прямо сейчас:

⏰ **Парсер работает** каждые 5 минут  
🔍 **Ищет новые источники** каждые 30 минут  
🤖 **ML анализирует** каждую новость  
📱 **Telegram отправляет** топ новости  
📊 **Дашборд обновляется** в реальном времени  

---

**Версия:** 1.0.0  
**Статус:** ✅ Production Ready  
**Дата:** 28 ноября 2025

**Приятного использования! 🚀**



























