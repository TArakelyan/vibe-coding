# 🎰 Парсер новостей букмекерской индустрии

## Описание

Профессиональная система автоматического сбора и анализа новостей о букмекерской индустрии и игорном бизнесе в России. Система работает 24/7, собирает новости каждые 5 минут, динамически находит новые источники и отправляет уведомления в Telegram.

## 🚀 Возможности

### Основной функционал
- ⚡ **Автоматический парсинг** каждые 5 минут из 200+ источников
- 🔍 **Динамический поиск источников** через Google/Yandex каждые 30 минут
- 📱 **Telegram уведомления** о важных новостях
- 🎯 **Умная фильтрация** по 100+ ключевым словам
- 🏷️ **Автоматическая категоризация** новостей
- ⚠️ **Оценка срочности** (0-10) с учетом контекста
- 🧠 **ML-классификация** с использованием NLP

### Технические возможности
- 🐳 **Docker** контейнеризация для легкого развертывания
- 💾 **PostgreSQL** для надежного хранения данных
- ⚡ **Redis** для быстрой дедупликации
- 📊 **Веб-дашборд** с графиками и статистикой в реальном времени
- 🔄 **Celery** для фоновых задач
- 🌐 **Nginx** для проксирования запросов
- 📈 **Plotly** графики для визуализации

### Источники новостей
- Sports.ru (букмекерская индустрия)
- LegalBet.ru
- BetonMobile.ru
- RBC.ru (спортивный раздел)
- Коммерсантъ (игорный бизнес)
- CNews.ru
- RSS фиды всех легальных БК
- Динамически найденные источники через поиск

### Категории новостей
1. **Регулирование** - ФНС, лицензии, ЦУПИС, СРО
2. **Законодательство** - Госдума, правительство, законопроекты
3. **Судебные дела** - суды, блокировки, штрафы
4. **Налоги** - GGR, налогообложение
5. **Компании** - букмекеры, новости БК
6. **Рынок** - обороты, выплаты, статистика
7. **Продукты** - приложения, новые сервисы

## 📦 Установка

### Быстрый старт с Docker

1. Клонируйте репозиторий:
```bash
git clone <repository>
cd betting-news-parser
```

2. Создайте файл `.env`:
```bash
cp .env.example .env
```

3. Настройте `.env` файл:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

4. Запустите через Docker:
```bash
docker-compose up -d
```

5. Откройте дашборд:
```
http://localhost
```

### Локальная установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
python -m spacy download ru_core_news_sm
```

2. Установите PostgreSQL и Redis

3. Создайте базу данных:
```bash
createdb betting_news
psql betting_news < init_db.sql
```

4. Запустите парсер:
```bash
python main.py
```

5. Запустите дашборд (в отдельном терминале):
```bash
python dashboard.py
```

## 🎛️ Конфигурация

### Основные параметры (config.py)

```python
# Интервалы работы
PARSE_INTERVAL_MINUTES = 5        # Парсинг каждые 5 минут
SOURCE_DISCOVERY_INTERVAL_MINUTES = 30  # Поиск источников каждые 30 минут

# Параллельность
MAX_CONCURRENT_REQUESTS = 100     # Одновременных запросов

# Telegram
TELEGRAM_BOT_TOKEN = 'your_token'
TELEGRAM_CHAT_ID = 'your_chat_id'
```

### Добавление новых источников

Добавьте URL в `config.py`:
```python
PRIORITY_SOURCES = [
    'https://your-source.ru/news',
    # ...
]
```

### Добавление ключевых слов

Редактируйте словарь `KEYWORDS` в `config.py`:
```python
KEYWORDS = {
    'category_name': ['слово1', 'слово2', ...],
}
```

## 📊 Веб-дашборд

Дашборд доступен по адресу `http://localhost:5000` и включает:

- 📈 **Статистика** - общее количество новостей за 24ч/всего, активных источников
- 📉 **График timeline** - новости по времени за последние 48 часов
- 🥧 **Круговая диаграмма** - распределение по категориям
- 📊 **Топ источников** - bar chart самых активных источников
- 🏷️ **Облако тегов** - популярные ключевые слова
- 📰 **Список новостей** - последние новости с фильтрацией

## 🤖 Telegram Bot

### Настройка

1. Создайте бота через @BotFather
2. Получите token
3. Узнайте chat_id вашего канала/чата
4. Добавьте в `.env`:
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890
```

### Формат сообщений

```
🔥 Заголовок новости

📌 Категория: regulation
⚡ Срочность: 8.5/10
🔗 Читать полностью

Краткое описание новости...

#букмекеры #новости #regulation
```

## 🔧 API Endpoints

### Статистика
```
GET /api/stats
```
Возвращает общую статистику: количество новостей, источников, топ категории.

### Новости
```
GET /api/news?limit=50&category=regulation
```
Параметры:
- `limit` - количество новостей (по умолчанию 50)
- `category` - фильтр по категории (опционально)

### Графики
```
GET /api/chart/timeline
GET /api/chart/categories
GET /api/chart/sources
```

### Ключевые слова
```
GET /api/keywords
```
Возвращает топ-30 ключевых слов за последние 24 часа.

## 🧠 ML-классификация

Система использует NLP для анализа новостей:

### Извлечение сущностей (NER)
- Организации (букмекеры, регуляторы)
- Персоны (руководители, чиновники)
- Локации
- Денежные суммы
- Даты

### Анализ тональности
- Позитивная / Негативная / Нейтральная
- Влияет на оценку срочности

### Автоклассификация
- TF-IDF векторизация
- Naive Bayes классификатор
- Обучение на размеченных данных

## 📝 Структура проекта

```
betting-news-parser/
├── main.py                 # Главный файл запуска
├── config.py              # Конфигурация
├── database.py            # Работа с PostgreSQL
├── parser.py              # Основной парсер
├── source_discovery.py    # Динамический поиск источников
├── telegram_bot.py        # Telegram уведомления
├── ml_classifier.py       # ML-классификация
├── tasks.py               # Celery задачи
├── dashboard.py           # Веб-дашборд
├── templates/
│   └── dashboard.html     # HTML шаблон дашборда
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile            # Docker образ
├── init_db.sql           # Инициализация БД
├── nginx.conf            # Nginx конфигурация
├── requirements.txt      # Python зависимости
└── README.md            # Документация
```

## 🔄 Фоновые задачи (Celery)

Система автоматически выполняет:

1. **Парсинг новостей** - каждые 5 минут
2. **Поиск источников** - каждые 30 минут
3. **Отправка уведомлений** - каждые 10 минут
4. **Ежедневная сводка** - каждый день в 9:00
5. **Очистка данных** - каждый день в 3:00 (удаление старше 30 дней)

## 📈 Масштабирование

### Увеличение производительности
- Увеличьте `MAX_CONCURRENT_REQUESTS` в config.py
- Добавьте больше Celery workers
- Используйте прокси-пулы для избежания блокировок

### Добавление источников
- Система автоматически находит новые источники
- Можно добавить вручную в `PRIORITY_SOURCES`
- Поддержка RSS фидов

### Распределенная архитектура
- Можно запустить несколько парсеров на разных серверах
- Общая база данных PostgreSQL
- Общий Redis для дедупликации

## 🐛 Отладка

### Просмотр логов
```bash
# Docker
docker-compose logs -f parser

# Локально
tail -f logs/parser_*.log
```

### Проверка БД
```bash
docker-compose exec postgres psql -U parser_user -d betting_news
```

### Проверка Redis
```bash
docker-compose exec redis redis-cli
```

## 🔒 Безопасность

- Все пароли в `.env` файле
- `.env` добавлен в `.gitignore`
- Используйте strong passwords для production
- Настройте firewall для БД и Redis
- Используйте HTTPS для дашборда в production

## 📊 Мониторинг

- Веб-дашборд показывает статистику в реальном времени
- Логи записываются в `logs/`
- Celery beat показывает расписание задач
- PostgreSQL имеет встроенные метрики

## 🤝 Вклад

Система полностью модульная и расширяемая:
- Добавляйте новые источники в `config.py`
- Создавайте новые парсеры в `parser.py`
- Добавляйте ML модели в `ml_classifier.py`
- Расширяйте API в `dashboard.py`

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано для мониторинга букмекерской индустрии России



























