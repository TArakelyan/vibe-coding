# 🚀 Быстрый старт

## Запуск через Docker (рекомендуется)

### 1. Подготовка

```bash
# Скопируйте пример конфигурации
cp .env.example .env
```

### 2. Настройка Telegram (опционально)

Отредактируйте `.env`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

Как получить:
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен
3. Отправьте сообщение боту
4. Получите chat_id через [@userinfobot](https://t.me/userinfobot)

### 3. Запуск

**Linux/macOS:**
```bash
chmod +x run.sh
./run.sh
```

**Windows PowerShell:**
```powershell
docker-compose up -d
```

### 4. Доступ к дашборду

Откройте в браузере: http://localhost

### 5. Просмотр логов

```bash
docker-compose logs -f parser
```

### 6. Остановка

```bash
docker-compose down
```

## Запуск локально (без Docker)

### 1. Требования

- Python 3.9+
- PostgreSQL 14+
- Redis 6+

### 2. Установка

```bash
# Установка зависимостей
python setup_local.py
```

### 3. Настройка базы данных

```bash
# Создание БД
createdb betting_news

# Инициализация схемы
psql betting_news < init_db.sql
```

### 4. Запуск Redis

```bash
redis-server
```

### 5. Запуск парсера

```bash
# Терминал 1: Парсер
python main.py

# Терминал 2: Дашборд
python dashboard.py
```

### 6. Тестирование

```bash
python test_parser.py
```

## Структура проекта

```
betting-news-parser/
├── main.py                 # Главный скрипт
├── parser.py              # Парсер новостей
├── source_discovery.py    # Поиск источников
├── telegram_bot.py        # Telegram уведомления
├── ml_classifier.py       # ML классификация
├── dashboard.py           # Веб-дашборд
├── database.py            # БД операции
├── tasks.py               # Celery задачи
├── config.py              # Конфигурация
├── docker-compose.yml     # Docker конфигурация
└── README.md             # Полная документация
```

## Ключевые возможности

✅ **Автоматический парсинг** каждые 5 минут  
✅ **200+ источников** новостей  
✅ **Динамический поиск** новых источников  
✅ **Telegram уведомления** о важных событиях  
✅ **ML классификация** с NLP анализом  
✅ **Веб-дашборд** с графиками в реальном времени  
✅ **Дедупликация** через Redis  
✅ **PostgreSQL** для надежного хранения  

## Категории новостей

- 📋 **Регулирование** - ФНС, лицензии, ЦУПИС
- ⚖️ **Законодательство** - законопроекты, Госдума
- 👨‍⚖️ **Судебные дела** - суды, штрафы, блокировки
- 💰 **Налоги** - GGR, налогообложение
- 🏢 **Компании** - букмекеры, новости БК
- 📊 **Рынок** - обороты, статистика
- 📱 **Продукты** - приложения, сервисы

## Мониторинг

### Веб-дашборд
- **URL:** http://localhost:5000
- **Статистика:** общее количество новостей, источников
- **Графики:** timeline, категории, источники
- **Ключевые слова:** облако тегов
- **Список новостей:** с фильтрацией

### API Endpoints

```bash
# Статистика
curl http://localhost:5000/api/stats

# Новости
curl http://localhost:5000/api/news?limit=10

# Графики
curl http://localhost:5000/api/chart/timeline
curl http://localhost:5000/api/chart/categories
curl http://localhost:5000/api/chart/sources

# Ключевые слова
curl http://localhost:5000/api/keywords
```

## Расписание задач

| Задача | Периодичность |
|--------|--------------|
| Парсинг новостей | Каждые 5 минут |
| Поиск источников | Каждые 30 минут |
| Telegram уведомления | Каждые 10 минут |
| Ежедневная сводка | 9:00 каждый день |
| Очистка данных | 3:00 каждый день |

## Troubleshooting

### Docker не запускается

```bash
# Проверьте статус
docker-compose ps

# Пересоберите
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### База данных не подключается

```bash
# Проверьте контейнер PostgreSQL
docker-compose logs postgres

# Перезапустите
docker-compose restart postgres
```

### Парсер не находит новости

1. Проверьте интернет-соединение
2. Убедитесь что источники доступны
3. Проверьте логи: `docker-compose logs parser`

### Telegram не работает

1. Проверьте токен и chat_id в `.env`
2. Убедитесь что бот добавлен в канал/чат
3. Проверьте логи

## Полезные команды

```bash
# Просмотр всех контейнеров
docker-compose ps

# Логи конкретного сервиса
docker-compose logs -f [service_name]

# Перезапуск сервиса
docker-compose restart [service_name]

# Подключение к БД
docker-compose exec postgres psql -U parser_user -d betting_news

# Подключение к Redis
docker-compose exec redis redis-cli

# Выполнение команды в контейнере
docker-compose exec parser python test_parser.py
```

## Поддержка

Для вопросов и предложений создавайте Issue в репозитории.



























